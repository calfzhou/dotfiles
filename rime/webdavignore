# webdav 同步中可根据以下正则忽略匹配的文件
# 注意：
# 1. 本文件使用 UTF-8 格式；
# 2. 已 `#` 开头的为注释信息，不起任何作用；
# 3. 本文件的正则匹配，与 git 中的 .gitignore 文件中的表达式是有区别的，两者不通用。

# 忽略已 `.plist` 结尾的文件
.*\.plist$

# 忽略路径中包含 `build` 文件夹及文件夹下的全部文件
# /build/*

# 忽略路径中 `others` 文件夹及文件夹下的全部文件
/others/*

# 忽略已 `.bin` 结尾的文件
# .*\.bin$
